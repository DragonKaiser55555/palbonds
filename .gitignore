# Never track live game logs
*.log

# Local archive of material that is no longer part of the product (moved here
# 2026-09-11). Nothing was deleted — it holds the three third-party reference
# mods (PalFollowerTweaks, Passive Pals, PassiveWildPals — downloadable again
# if a future question needs them), the repak mapping data for FModel
# (research/Mappings.usmap), the pre-release dev-install backup, the duplicate
# images.zip, and the dev-only research modules (Spy, OtomoWatch, InputSpy)
# that were never initialised by main.lua.
stale/

# Test-harness dependencies. `npm install fengari` in tools/harness restores
# them; see tools/harness/README.md.
tools/harness/node_modules/
tools/harness/package-lock.json
